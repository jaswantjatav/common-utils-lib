{
  "name": "common-utils-lib",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "registry": "https://trialdmzd5x.jfrog.io/npm-virtual/"
  }
}